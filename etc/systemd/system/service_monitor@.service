[Unit]
After=network.target dbus.socket

[Service]
Type=simple
Environment="TG_BOT_ID=?"
Environment="TG_BOT_TOKEN=?"
ExecStart=/usr/local/bin/service_monitor.sh %I "${TG_CHAT_ID}"

[Install]
WantedBy=multi-user.target
